# RADISH DECK

# ELEMENT SYSTEM

Версия:

Alpha 0.1.0

------------------------------------------------------------------------

# 1. Назначение

Element — универсальный визуальный объект панели управления.
Целевой контракт определяется Vision v3, Architecture v3 и Designer Specification.
Этот документ описывает развитие системы, а не текущую схему deck.v1.json.

# 2. Общая структура элемента

    Element
    ├── Id / Type / Name
    ├── Layout       (Position, Width, Height, Layer)
    ├── Content      (Text, Image, Icon)
    ├── Appearance   (Background, Border, Radius, Shadow, Opacity)
    ├── Behavior     (Animation, States)
    └── Action       (привязка при необходимости)

Действие не обязательно для декоративного элемента. Состояния относятся
к Behavior, визуальные настройки — к Appearance. Отдельный обязательный
верхнеуровневый Style не является целевым контрактом.
# 3. Типы элементов

Базовые элементы Designer: Button, Text, Image, Container. Vision также предусматривает Logo и Widget. Indicator, Slider и Toggle остаются будущими расширениями.

## Button

Основной элемент управления.

Используется для:

-   запуска действий;
-   переходов между страницами;
-   управления системой.

Пример:

    Button:

    Name:
    Restart PC


    Action:
    restart_system

Свойства:

-   название;
-   иконка;
-   действие;
-   размер;
-   состояние.

------------------------------------------------------------------------

# 4. Text

Элемент отображения текста.

Используется для:

-   заголовков;
-   информации;
-   статусов.

Пример:

    CPU Usage

    45%

Свойства:

-   текст;
-   размер;
-   выравнивание;
-   стиль.

------------------------------------------------------------------------

# 5. Image

Отображение изображения.

Используется для:

-   иконок;
-   логотипов;
-   ассетов.

Свойства:

-   путь;
-   размер;
-   масштабирование.

------------------------------------------------------------------------

# 6. Indicator

Элемент состояния.

Главная задача:

Показать состояние системы.

Примеры:

Docker:

    Docker

    Running

Server:

    Server

    Online

Состояния:

-   Online;
-   Offline;
-   Running;
-   Stopped;
-   Error.

------------------------------------------------------------------------

# 7. Widget

Динамический элемент.

Получает данные от системы.

Примеры:

## CPU Widget

Показывает:

-   загрузку CPU;
-   температуру.

## RAM Widget

Показывает:

-   использование памяти.

## Network Widget

Показывает:

-   скорость;
-   состояние соединения.

------------------------------------------------------------------------

# 8. Управляющие элементы

## Slider

Пример:

Громкость:

    Volume

    ██████░░

    60%

Свойства:

-   min;
-   max;
-   value.

------------------------------------------------------------------------

## Toggle

Переключатель.

Пример:

    Docker Auto Start

    ON

Состояния:

-   enabled;
-   disabled.

------------------------------------------------------------------------

# 9. Layout

Элемент располагается на Canvas. Layout описывает Position X/Y, Width,
Height и Layer. Canvas задаёт размеры, ориентацию, фон и ресурсы.
Device Profiles определяют правила отображения для Desktop, Tablet,
Mobile и Custom. Сетка — инструмент редактирования, а не ограничение модели.

Существующие GridX/GridY/GridWidth/GridHeight относятся к текущему
прототипу deck.v1. Переход к целевой модели требует отдельного решения
о совместимости; этот документ не меняет формат хранения.
# 10. State System

Каждый элемент может иметь состояние.

Button:

    Normal
    Hover
    Running
    Success
    Error
    Pressed

    Disabled

Docker:

    Running

    Stopped

    Error

------------------------------------------------------------------------

# 11. Action Binding

Элемент может выполнять действие.

Связь:

    Element

    ↓

    Action

    ↓

    Executor

    ↓

    Result

    ↓

    State Update

Пример:

Кнопка:

    Start Docker

Action:

    docker.start

Result:

    Docker Running

------------------------------------------------------------------------

# 12. Properties

Каждый элемент имеет настройки.

Пример Button:

-   Name;
-   Icon;
-   Action;
-   Size;
-   Color;
-   Permission.

------------------------------------------------------------------------

# 13. Events

Элемент поддерживает события.

Alpha:

-   Click

Будущее:

-   Double Click;
-   Long Press;
-   Swipe;
-   Gesture.

------------------------------------------------------------------------

# 14. Renderer

Один Renderer отвечает за Desktop Preview и Web Runtime. При одинаковом профиле устройства результат должен совпадать.

Element не знает:

-   Desktop;
-   Web;
-   Mobile.

Он предоставляет:

-   данные;
-   состояние;
-   свойства.

------------------------------------------------------------------------

# 15. Правила

Запрещено:

-   создавать отдельный элемент для каждого клиента;
-   хранить дизайн внутри кода;
-   связывать элемент напрямую с Windows API.

Правильно:

    Element

    ↓

    Action

    ↓

    Executor

------------------------------------------------------------------------

# 16. Цель системы элементов

Создать универсальные компоненты.

Один элемент должен работать в:

-   Desktop;
-   Web;
-   Mobile;
-   Tablet.
