async function load(){const r=await fetch('/deck');const d=await r.json();const root=document.getElementById('deck');root.className='deck';for(const p of d.pages||[]){for(const e of p.elements||[]){const b=document.createElement('button');b.className='btn';b.textContent=e.name||e.actionId;b.onclick=()=>fetch('/execute',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({elementId:e.id})});root.appendChild(b)}}}load();